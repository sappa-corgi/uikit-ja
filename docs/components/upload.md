# コンポーネント: アップロード（Upload）

ファイルアップロードを扱うためのコンポーネント。ドラッグ＆ドロップやプログレス、複数ファイル対応などの機能があります。

例（基本的な設定）:

```html
<form id="upload" uk-form-custom>
  <input type="file">
</form>
```

ポイント:
- サーバーへの送信は通常 JavaScript 側でハンドリングする。
- 進捗表示やエラーハンドリングを組み合わせる。
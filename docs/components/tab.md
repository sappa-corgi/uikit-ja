# コンポーネント: タブ（Tab）

タブインターフェースを作成するためのコンポーネント。`uk-tab` と `uk-switcher` を組み合わせて使うことが多い。

例:

```html
<ul class="uk-tab" uk-tab>
  <li><a href="#">Tab1</a></li>
  <li><a href="#">Tab2</a></li>
</ul>
<ul class="uk-switcher">
  <li>コンテンツ1</li>
  <li>コンテンツ2</li>
</ul>
```

ポイント:
- レスポンシブで列挙スタイルを変えられる。
# コンポーネント: スティッキー（Sticky）

スクロールに応じて要素を固定（sticky）するためのコンポーネント。ヘッダーやサイドバーでよく使われます。

例:

```html
<div uk-sticky="top: 0">
  <nav>固定メニュー</nav>
</div>
```

ポイント:
- 固定の解除ポイントやクラスを設定できる。ブラウザの挙動に応じたフォールバックがある。
# コンポーネント: テーブル（Table）

テーブル表示用のスタイルを提供します。レスポンシブやストライプ、境界線などのユーティリティが利用可能です。

例:

```html
<table class="uk-table uk-table-divider">
  <thead>
    <tr><th>ヘッダ</th></tr>
  </thead>
  <tbody>
    <tr><td>データ</td></tr>
  </tbody>
</table>
```

ポイント:
- `uk-table` 系クラスで装飾を調整する。
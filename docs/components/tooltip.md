# コンポーネント: ツールチップ（Tooltip）

要素に対して小さな補助情報を表示するツールチップ。マウスオーバーやフォーカスで表示されます。

例:

```html
<button uk-tooltip="タイトル: ツールチップ">Hover</button>
```

ポイント:
- 位置やアニメーションをオプションで制御できる。アクセシビリティに配慮して `aria-*` を合わせて使う。